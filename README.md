# InsoDocs Landing Page

Standalone landing page for `insodocs.app`.

## Render

- Service type: Static Site
- Build command: blank
- Publish directory: `landing-site`
- Root document: `index.html`

The repository includes a `render.yaml` blueprint at the project root.
